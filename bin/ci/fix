#!/usr/bin/env bash

set -eu

readonly ROOT_PATH="$(realpath "$(dirname "$(realpath "$0")")/../..")"
source "${ROOT_PATH}"/bin/common.inc.sh
source "${ROOT_PATH}"/bin/dockerise.inc.bash

"${ROOT_PATH}"/bin/ci/phpcbf "${@}" || true
"${ROOT_PATH}"/bin/ci/validate
